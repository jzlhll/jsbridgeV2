<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:resizeableActivity="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.Material3.DayNight.NoActionBar"
        tools:targetApi="31">

        <activity
            android:name=".EntroActivity"
            android:exported="true"
            android:label="AllanComponents">
            <meta-data
                android:name="android.app.lib_name"
                android:value="" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".WebBridgeActivity">
        </activity>
        <activity
            android:name=".activities.WebEChartsGetDataUrlBitmapActivity">
        </activity>
        <activity
            android:name=".activities.WebEChartsHtmlRtcRecordActivity">
        </activity>
        <activity
            android:name=".activities.WebEChartsWebViewSoftRecordActivity">
        </activity>
        <activity
            android:name=".activities.WebVideoPlayActivity">
        </activity>
    </application>

</manifest>